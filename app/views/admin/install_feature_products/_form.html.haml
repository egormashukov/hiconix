= form_for [:admin, InstallFeatureProduct.new], builder: AdminAddonFormBuilder, remote: true do |f|
  .row
    .col-md-9= f.select :install_feature_id, options_for_select(InstallFeature.has_not_that_product(product.id)), {}, class: "select2"
    .col-md-3
      = f.hidden_field :product_id, value: product.id
      = f.submit t(:add), class: 'btn btn-primary'
%br